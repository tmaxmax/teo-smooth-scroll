'use strict';

/**
 * Logs "sarmale" to console, along with a chosen number.
 * @param {number} elem random number at choice
 */
function sarmale(elem) {
  console.log(`sarmale ${elem}`);
}

sarmale(3);
